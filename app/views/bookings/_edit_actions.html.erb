 <%= simple_form_for(booking, html: { class: "text-start" }) do |f| %>
  <%= f.button :submit, class: 'btn btn-flat', style: 'font-weight: bold; margin-right: 10px', value: 'Accept' %>
  <%= f.button :submit, class: 'btn btn-flat', style: 'font-weight: bold; margin-right: 10px', value: 'Reject' %>
<% end %>
